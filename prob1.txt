define prefix {
	lambda inL { lambda outI {
	
		int rawLength = getLength inL;
		str[rawLength] rawList = unpackL inL;
		
		int sLength = getSLength rawList;
		str[sLength] sList = sort rawList;
		
		//Need to figure out exactly how to do nested lambdas
		str outS = createOutput sList outI;
		return outS;
		
	}}
}

define getLength {
	lambda inL {
		
	}
}